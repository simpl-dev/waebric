module test004

def main
  let
  	a = 1;
	b = 2;
	c = 3;
  in
  if (a || b && c && d)
	  echo "x";
  end
  echo "abc<em "bla">def<em "bla2">ghi<em em "a" + "b">";
  foobar echo "Hello, pretty!";
end

def foobar
	p "hello";
end

