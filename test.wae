module test004

def main
  if (a && b && c && !d)
	  echo "x";
  echo "abc<em "bla">def<em "bla2">ghi<em em "a" + "b">";
  foobar echo "Hello, pretty!";
end

def foobar
	p "hello";
end

def foo
	main;
	foo bar baz bag;
	foo("foo") echo "bar";
end

def x(a)
	blah;
end

def y(a,b)
	blah;
end
